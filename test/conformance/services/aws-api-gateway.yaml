provider: aws
service: API Gateway
checks:
  api_compat: GOCACHE=/private/tmp/exit-gafam-go-build go test ./test/compat -run APIGateway
evidence:
  target: local API Gateway compatibility adapter seed; Kong is not deployed
  app_change_mode: local adapter seed
  api_compat_covers: sdk_lifecycle,aws_cli_smoke,terraform_smoke,boto3_smoke,rest_api_lifecycle,resources,methods,integrations,deployments,stages,stage_update,custom_domains,tags,pagination,quota,authz,audit
