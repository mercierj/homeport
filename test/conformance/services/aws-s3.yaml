provider: aws
service: S3
checks:
  api_compat: GOCACHE=/private/tmp/exit-gafam-go-build go test ./test/compat -run S3
evidence:
  target: local S3 compatibility adapter seed; MinIO is not deployed
  app_change_mode: local adapter seed
  api_compat_covers: sdk_lifecycle,aws_cli_smoke,terraform_smoke,boto3_smoke,bucket_lifecycle,object_lifecycle,object_pagination,bucket_tags,tag_schema_validation,authz,audit,idempotency,object_quota,dependency_lifecycle
