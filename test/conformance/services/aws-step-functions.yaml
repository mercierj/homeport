provider: aws
service: Step Functions
checks:
  api_compat: GOCACHE=/private/tmp/exit-gafam-go-build go test ./test/compat -run StepFunctions
evidence:
  target: local Step Functions compatibility adapter seed; Temporal is not deployed
  app_change_mode: adapter
  api_compat_covers: sdk_lifecycle,validation,pagination,quota,authz
