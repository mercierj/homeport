provider: aws
service: CodeBuild
checks:
  api_compat: GOCACHE=/private/tmp/exit-gafam-go-build go test ./test/compat -run CodeBuild
evidence:
  target: local CodeBuild compatibility adapter seed; GitLab CI is not deployed
  app_change_mode: local adapter seed
  api_compat_covers: sdk_project_lifecycle,descending_list
