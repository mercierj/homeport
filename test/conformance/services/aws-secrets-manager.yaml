provider: aws
service: Secrets Manager
checks:
  api_compat: GOCACHE=/private/tmp/exit-gafam-go-build go test ./test/compat -run SecretsManager
evidence:
  target: local Secrets Manager compatibility adapter seed; Vault is not deployed
  app_change_mode: local adapter seed
  api_compat_covers: sdk_lifecycle,aws_cli_smoke,terraform_smoke,boto3_smoke,secret_lifecycle,secret_version,put_version_idempotency,resource_policy,tags,pagination,quota,authz,audit
