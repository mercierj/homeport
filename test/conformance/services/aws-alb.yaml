provider: aws
service: ALB
checks:
  discover: GOCACHE=/private/tmp/exit-gafam-go-build go test ./internal/infrastructure/mapper/networking -run TestALBConformanceManagedAToZ
  api_compat: GOCACHE=/private/tmp/exit-gafam-go-build go test ./test/compat -run TestALBCompatibilityAdapter -count=1
evidence:
  target: Traefik with HomePort ALB compatibility adapter seed
  app_change_mode: local adapter seed
  api_compat_covers: sdk_lifecycle,pagination,authz,audit,quota,validation,post_only
