provider: aws
service: CloudWatch
checks:
  api_compat: GOCACHE=/private/tmp/exit-gafam-go-build go test ./test/compat -run CloudWatchLogs
evidence:
  target: local CloudWatch Logs compatibility adapter seed; Loki is not deployed
  app_change_mode: local adapter seed
  api_compat_covers: provider_errors,pagination,quota,authz,audit,retention,tags,sdk_lifecycle,aws_cli_smoke,terraform_smoke,boto3_smoke
