provider: aws
service: AppSync
checks:
  api_compat: GOCACHE=/private/tmp/exit-gafam-go-build go test ./test/compat -run AppSync
evidence:
  target: local AppSync compatibility adapter seed; Hasura is not deployed
  app_change_mode: local adapter seed
