# AWS Comprehend Migration

The local adapter supports document-classifier create, describe, list, and delete through `/compat/aws/comprehend`. It is in-memory and does not prove a deployed spaCy backend, model migration, persistence, backup, cutover, or rollback.
